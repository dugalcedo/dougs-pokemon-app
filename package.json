{
  "name": "dougs-pokemon-app",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx concurrently \"cd backend && npx tsx --watch src/server.ts\" \"cd frontend && npm run dev\" -n \"BACK,FRONT\""
  },
  "author": "doug alcedo",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tsx": "^4.19.4"
  }
}
